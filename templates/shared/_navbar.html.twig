<nav class="navbar navbar-expand-sm navbar-dark bg-dark">
	<div class="container-fluid">
		{# <a class="navbar-brand" href="{{ path('home') }}">Mon projet</a> #}
		<button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'app_logout') ? 'active' : '' }}" aria-current="page" href="{{ path('app_logout') }}">Se déconnecter</a>				
					</li>
					<li>
						<span class="nav-link text-light">connecté en tant que : {{ app.user.email }}</span>
					</li>
				{% else %}	
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'app_register') ? 'active' : '' }}" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
					</li>
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'app_login') ? 'active' : '' }}" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
					</li>
				{% endif %}			
			</ul>
			<br>
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link {{ (app.request.attributes.get('_route') == 'home') ? 'active' : '' }}" aria-current="page" href="{{ path('home') }}">Accueil</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link {{ (app.request.attributes.get('_route') == 'profile_home') ? 'active' : '' }}" aria-current="page" href="{{ path('profile_home') }}">Profil</a>
					</li>
					{% if app.user.roles[0] == "ROLE_ADMIN" %}
						<li class="nav-item">
							<a class="bg-secondary nav-link {{ (app.request.attributes.get('_route') == 'admin_home') ? 'active' : '' }}" aria-current="page" href="{{ path('admin_home') }}">Admin</a>
						</li>
					{% endif %}
				{% endif %}
				
			</ul>
		</div>
	</div>
</nav>
